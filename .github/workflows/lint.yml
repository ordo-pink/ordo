name: Lint
on: push

jobs:
  lint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v3
      - name: Boot
        run: boot/boot.sh
      - name: Run ESLint
        run: opt/bun eslint lib

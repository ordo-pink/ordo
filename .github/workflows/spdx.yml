name: SPDX
on: push

jobs:
  spdx:
    name: Check SPDX Records
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v3
      - name: Boot
        run: boot/boot.sh
      - name: Run SPDX Records Check
        run: bin/dog spdx -bn

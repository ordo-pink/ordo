// TODO: Put all RRRs here
export enum RRR {
	METADATA_NOT_LOADED,
	METADATA_NOT_FOUND,
	INVALID_FSID,
	FSID_ALREADY_TAKEN,
	FSID_NOT_FOUND,
	INVALID_NAME,
	NAME_PARENT_TAKEN,
	INVALID_PARENT,
	INVALID_ANCESTOR,
	INVALID_DESCENDENT,
	INVALID_CHILD,
	PARENT_NOT_FOUND,
	LINK_DOES_NOT_EXIST,
	CIRCULAR_PARENT_REFERENCE,
	INVALID_LINK,
	INVALID_LABEL,
	INVALID_SIZE,
	INVALID_TYPE,
	INVALID_PROPS,
	USERS_NOT_LOADED,
}

export const toRRR =
	<_TKey extends keyof typeof RRR>(key: _TKey) =>
	() =>
		RRR[key] as const

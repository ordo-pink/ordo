export enum COMMAND_PALETTE_SECTION {
	ITEMS,
	PINNED_ITEMS,
}

export const FUZZY_CHECK_RATIO = 0.7

export const enum COMMAND_PALETTE_TOGGLE_COMMAND {
	ID = "command_palette.toggle",
	HOTKEY = "mod+shift+p",
}

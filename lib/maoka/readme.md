# Maoka

### TailwindCSS Intellisense VSCode Plugin

```json
"tailwindCSS.experimental.classRegex": [
  "set_class\\(([^`]*)",
  "add_class\\(([^`]*)",
  "remove_class\\(([^`]*)",
  "replace_class\\(([^`]*)"
]
```

/**
 * Native theme to be used in the browser window.
 */
export enum ColourTheme {
  /**
   * Depend on the environment preferences.
   */
  SYSTEM = "system",

  /**
   * Light theme.
   */
  LIGHT = "light",

  /**
   * Dark theme.
   */
  DARK = "dark",
}

meta {
  name: Get File Modification Time
  type: http
  seq: 2
}

head {
  url: {{DT_HOST}}/:uid/:fsid
  body: none
  auth: bearer
}

params:path {
  uid: {{CURRENT_ID}}
  fsid: {{CURRENT_ID}}
}

headers {
  Origin: {{ORIGIN}}
}

auth:bearer {
  token: {{CURRENT_TOKEN}}
}

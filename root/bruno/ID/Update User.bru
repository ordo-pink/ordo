meta {
  name: Update User
  type: http
  seq: 5
}

patch {
  url: {{ID_HOST}}/users/{{USER_ID}}
  body: json
  auth: none
}

headers {
  Origin: {{ORIGIN}}
}

body:json {
  {
    "handle": "{{USER_HANDLE}}",
    "email": "{{USER_EMAIL}}"
  }
}

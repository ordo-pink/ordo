export enum BlockNodeType {
	LINE = "line",
	EMBED = "embed",
	BLOCKQUOTE = "blockquote",
	HEADING = "heading",
	COMPONENT = "component",
	HR = "hr",
	TODO = "todo",
}

export enum InlineNodeType {
	TAG = "tag",
	TEXT = "text",
	LINK = "link",
	BOLD = "bold",
	CODE = "code",
	ITALIC = "italic",
	STRIKETHROUGH = "strikethrough",
}
